# SQL Generation

This component contains the prompt used to generate SQL queries for retrieving structured data.

The prompt was designed to handle larger data volumes efficiently by leveraging SQL-based data retrieval, improving performance and enabling scalable analytics workflows.
