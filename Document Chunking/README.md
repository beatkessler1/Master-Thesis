# Document Chunking

This component contains a Python script used to extract and transform documents into text chunks for downstream processing with Large Language Models (LLMs).

The chunking process was designed to support scalable document processing and prepare content for embedding and retrieval workflows. The implementation includes parallelized processing using threading, accelerating the extraction process by up to 8×.

This folder includes:
- A Python script for document parsing and chunk extraction
